import React from 'react';
import InputApiKey from '@/components/inputApiKey';

export default function Page() {
  return (
    <main>
      <InputApiKey /> {/* <- Render Home component */}
    </main>
  );
}
